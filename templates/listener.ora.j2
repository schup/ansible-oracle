# listener.ora Network Configuration File: {{ ora_user_env.ORACLE_HOME }}/network/admin/listener.ora
LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
      {% for ip in ansible_all_ipv4_addresses|sort -%}
{{ nl }}        (ADDRESS = (PROTOCOL = TCP)(HOST = {{ ip }})(PORT = 1521))
      {%- endfor %}
{{ nl }}
    )
  )
